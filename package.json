{
  "name": "@repris/vite-monorepo",
  "author": "Adi Unnithan",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "^18"
  },
  "packageManager": "pnpm@7.0.0",
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "typescript": "^4.7"
  },
  "scripts": {
    "build:tsc": "tsc -b .",
    "build:vite": "pnpm -r run --if-present --stream build:vite",
    "test": "pnpm -r run --if-present --stream test"
  }
}
