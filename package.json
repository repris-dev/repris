{
  "name": "@repris/monorepo",
  "author": "repris",
  "version": "0.1.0",
  "main": "index.js",
  "engines": {
    "node": "^18"
  },
  "packageManager": "pnpm@8.6.1",
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "typescript": "~4.9"
  },
  "scripts": {
    "build:tsc": "tsc -b .",
    "build:vite": "pnpm -r run --if-present --stream build:vite",
    "test": "pnpm -r run --if-present --stream test"
  }
}
